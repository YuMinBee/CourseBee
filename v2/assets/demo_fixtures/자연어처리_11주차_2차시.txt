자연어처리 11주차 2차시: 순차 모델링

RNN은 입력을 시간 순서대로 처리하면서 이전 hidden state를 다음 단계로 전달한다. 문장의 순서를 반영할 수 있지만 긴 문맥에서는 gradient vanishing 때문에 앞부분의 정보가 약해질 수 있다.

LSTM은 input gate, forget gate, output gate를 이용해 유지할 정보와 버릴 정보를 조절한다. 따라서 기본 RNN보다 장기 의존 관계를 다루는 데 유리하다. 토큰화된 입력은 임베딩을 거쳐 RNN 또는 LSTM 같은 순차 모델로 전달된다.
